import kotlin.Int;

CREATE TABLE IF NOT EXISTS spotify_track (
  `track_id` TEXT PRIMARY KEY NOT NULL,
  `artist_name` TEXT,
  `album_name` TEXT,
  `track_name` TEXT,
  `track_length` INTEGER AS Int
);

insert:
INSERT OR IGNORE INTO spotify_track
VALUES ?;
