package ly.david.musicsearch.shared.feature.graph.viz.timer

/**
 * used to make the timer sleep until next frame or event.
 */
internal actual fun setTimeout(
    handler: () -> Unit,
    timeout: Int,
): Any {
    TODO("Not yet implemented")
}